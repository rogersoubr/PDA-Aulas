export class Game{
    constructor(id,name, year, gender, plataforms){
        (id = this.id),
        (name = this.name),
        (year = this.year),
        (gender = this.gender),
        (plataforms = this.plataforms)
    }

}