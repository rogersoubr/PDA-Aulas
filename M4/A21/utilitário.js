// Atividade 1: Criem uma função que inverta uma string
// Entrada: "abc" | Saida esperada: "cba"

function inverteString(str) {
  const inverse = str.split('').reverse(''). 
  return
}

// Atividade 2: Criem uma função que verifique se a palavra é um palíndromo
// Entrada: "arara" | Saida esperada: true

function ehPalindromo(str) {
  return
}

// Atividade 3: Criem uma função que conte o número de ocorrências de um elemento em um array
// Entrada: [1, 2, 3, 1, 1], 1 | Saida esperada: 3

function contaOcorrencias(arr, elemento) {
  return
}